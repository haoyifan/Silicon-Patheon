__pycache__/
*.py[cod]
*.swp
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
replays/

# Per-match artifacts generated by silicon-match / silicon-serve.
runs/
runs-server/

# Ad-hoc coach files created at the repo root during local play.
coach_*.txt

.env
*.log

# Player strategy files — per-player secrets, not shared.
strategies/

# Agent-generated lessons — per-machine, not shared.
lessons/

# Personal deployment notes — keep real IPs / domains / hostnames OUT of repo.
# See docs/DEPLOY_HETZNER.md for the generic guide.
Silicon-Pantheon-Deployment.md

# System-test runtime configs — hold real IPs / domains / SSH targets.
# Only the *.example.toml files (with placeholder values) are tracked.
system_test*.toml
!system_test.example.toml
!system_test.remote.example.toml

# System-test run bundles — large, per-run, not worth tracking.
silicon-system-test-results/
*-Deployment.md
.silicon-pantheon/

# Claude Code — track skills and settings, ignore ephemeral state.
.claude/worktrees/
